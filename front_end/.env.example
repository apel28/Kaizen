# Frontend Environment Variables

# For local development, leave empty or use http://localhost:5001
VITE_API_BASE_URL=

# For production Netlify deployment, the API is served from the same domain
# No need to set this - it will default to window.location.origin
